import { FormatRuntimePipe } from './format-runtime-pipe';

describe('FormatRuntimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRuntimePipe();
    expect(pipe).toBeTruthy();
  });
});
